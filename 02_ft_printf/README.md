ft_handler_format = act like guard traffic datas

first, handler receiver the identifier s,d,i etc%

then pass through a tree of conditional verifications

test which conversion was asked 

type discovered, then handler delegates and aciona the right function for it.

Finally, handler return the total of characters printed

to mainten the right count for printf always exactly.